# MorseFleet Production Deployment Configuration
# Synology NAS deployment settings

# SSH connection to Synology
SYNOLOGY_HOST=straliadmin@station.strali.solutions

# Remote directory on Synology
REMOTE_DIR=/volume1/docker/morsefleet

# Docker container settings
CONTAINER_NAME=morsefleet
IMAGE_NAME=morsefleet:latest

# Port mapping (localhost only - exposed via Cloudflare tunnel)
# Using 3405 (3401-3404 are in use by other containers)
CONTAINER_PORT=3405:80

# Public URL (via Cloudflare tunnel)
SITE_URL=https://morsefleet.oeradio.at/
